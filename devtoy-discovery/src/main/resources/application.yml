spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: devtoy-discovery

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: ${SERVER_EUREKA_PORT:8070}
